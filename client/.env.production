# Loaded automatically by Vite when running `vite build` (production mode).
# In production, Express serves the React build itself, so the API lives on
# the same origin — a relative path avoids hardcoding any domain/port.
VITE_API_BASE_URL=/api
